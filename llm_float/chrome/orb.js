// orb.js - placeholder for future interaction
// Currently the orb is static; you can add click listeners etc.
document.addEventListener('DOMContentLoaded', () => {
  const orb = document.getElementById('orb');
  orb.addEventListener('click', () => {
    // Example: send a message to the extension background or open a popup
    console.log('Orb clicked');
  });
});